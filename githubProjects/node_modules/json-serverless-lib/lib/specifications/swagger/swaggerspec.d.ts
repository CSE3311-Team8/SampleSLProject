/// <reference types="express-serve-static-core" />
/// <reference types="passport" />
import express from 'express';
import { Spec, Schema, ApiKeySecurity, QueryParameter, Reference } from 'swagger-schema-official';
export declare class SwaggerSpec {
    private packageJsonPath;
    private packageInfo;
    private app;
    private predefinedSpec;
    private spec;
    constructor(packageInfoFilePath: string);
    private updateSpecFromPackage;
    private sortObject;
    initSpec(readOnly: boolean, basePath: string, routePath: string, specPath: string): Spec;
    getSpec: (app: express.Express, predefinedSpec: object, readOnly: boolean, basePath: string, apiRoutePath: string, specPath: string) => Spec;
    setSchemaReference(spec: Spec, definition: string): Reference;
    private getDefaultParameterSchema;
    getQueryParameterSchema(): QueryParameter[];
    private getDefaultPostResponses;
    private getDefaultPutResponses;
    private getDefaultDeleteResponses;
    private getDefaultSchemaProperties;
    addSchemaDefitions: (specification: Spec, schemaDefinitons: Schema) => Spec;
    addAuthentication: (specification: Spec, auth: ApiKeySecurity) => Spec;
}
//# sourceMappingURL=swaggerspec.d.ts.map