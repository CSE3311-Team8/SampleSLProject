import express from 'express';
import { SwaggerConfig } from './swagger.config';
import { ApiSpecification } from '../apispecification';
export declare class Swagger implements ApiSpecification {
    private swaggerSpec;
    private swaggerDefGen;
    private server;
    private config;
    private basePath;
    private apiRoutePath;
    private specPath;
    constructor(server: express.Express, config: SwaggerConfig, basePath: string, apiRoutePath: string, packageJsonFilePath: string, specPath: string);
    generateSpecification: (json: object) => object;
}
//# sourceMappingURL=swagger.d.ts.map