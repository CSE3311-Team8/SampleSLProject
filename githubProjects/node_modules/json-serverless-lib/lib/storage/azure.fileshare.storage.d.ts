import { StorageAdapter } from './storage';
import { AdapterAsync } from 'lowdb';
export declare class AzureFileShareStorageAdapter implements StorageAdapter {
    private azureStorageConnectionString;
    private shareName;
    private directoryName;
    private jsonFilePath;
    constructor(azureStorageConnectionString: string, shareName: string, directoryName: string, jsonFilePath: string);
    init(): AdapterAsync;
}
//# sourceMappingURL=azure.fileshare.storage.d.ts.map