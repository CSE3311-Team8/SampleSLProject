import { AuthStrategy } from './auth.strategy';
import express from 'express';
export declare class ApiKeyStrategy implements AuthStrategy {
    allowUrl: string[];
    server: express.Express;
    apiKey: string;
    authenticationMiddleware: any;
    constructor(server: express.Express, apiKey: string);
    init(): void;
}
//# sourceMappingURL=apikey.strategy.d.ts.map